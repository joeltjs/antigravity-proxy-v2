{
  "port": 20130,
  "host": "0.0.0.0",
  "strategy": "round-robin",
  "max_retries": 3,
  "api_endpoint": "https://daily-cloudcode-pa.googleapis.com/v1internal:streamGenerateContent",
  "oauth": {
    "token_url": "https://oauth2.googleapis.com/token",
    "_comment": "Optional: Configure OAUTH_ACCESS_KEY, OAUTH_SECRET_KEY, and OAUTH_REDIRECT_URI in .env to enable web-based Google OAuth login flow (similar to 9router). If left unconfigured, manual refresh token addition via dashboard/config remains functional."
  },
  "accounts": []
}
